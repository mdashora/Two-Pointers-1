// TC - O(n)
// SC - O(1)

// We use two pointers approach to solve this problem in optimized way.
// Find area between different lines and maintain max variable. 
class Container {
    public int maxArea(int[] height) {
        
        if(height==null || height.length==0){
            return 0;
        }

        int max=0;
        int left = 0;
        int right = height.length-1;

        while(left<right){

            // Calculate area of container by height * width where width is right-left index
            max = Math.max(max, Math.min(height[left], height[right]) * (right - left));

            if(height[left]<height[right]){
                left++;
            }
            else{
                right--;
            }
        }
        return max;
    }
}
